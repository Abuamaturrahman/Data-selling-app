name: data_selling_app
description: A Flutter application for selling mobile data and airtime.
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  get: ^4.6.5
  shared_preferences: ^2.0.15
  flutter_dotenv: ^5.0.2
  intl: ^0.18.0
  connectivity_plus: ^3.0.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env